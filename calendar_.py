#file name can't be same as calendar

import calendar
year = int(input("Enter the year:"))
month = 1
print (calendar.month(year,month))

print(calendar.calendar(year))
print(calendar.calendar(2001))

